# 批次1—批次3 整改落地 · 上线审批汇总

> 周期：批次1(P0 安全) → 批次2(P1 权限) → 批次3(P1 数据)，按既定顺序全部完成。
> 门禁：每批真实构建验证（后端 `mvn test` / 前端 `npm run build` + `npm test`），全程守绿基线。

## 一、整改完成情况（6/6 项）

| 批次 | 编号 | 整改项 | 落地要点 | 状态 |
|---|---|---|---|---|
| 1 | I-1 | 内存 token → JWT 会话化 | 新增零依赖 `JwtUtil`(HS256, uid/ver/jti/exp)；登出=jti 黑名单、改密/重置=账号级 token 版本自增实现集中失效；密钥外置 `app.security.token-secret` | ✅ |
| 1 | I-2 | localStorage XSS → HttpOnly Cookie | login 下发 `exam_token` HttpOnly+SameSite=Lax Cookie；CSP/nosniff/X-Frame-Options/Referrer-Policy；非 GET 跨源 Origin 校验(CSRF)；前端 withCredentials、localStorage 仅存非敏感 user | ✅ |
| 2 | I-3 | 老师写操作班域校验 + comparison 过滤 | GradeService `ensureTeacherClassScope`(create/update/delete/batch 越班 403)；StatisticsService.comparison 按可见班级过滤 | ✅ |
| 2 | I-4 | teacher-classes 跨校一致性 | TeacherClassService.assign 断言班级 schoolId==老师 schoolId，跨校 403 | ✅ |
| 3 | I-6 | findByStudentIdIn 下沉 source + 聚合索引 | 新增 `findByStudentIdInAndSource` 派生查询；Grade 复合索引 `idx_grade_student_source` | ✅ |
| 3 | I-7 | DataSeeder 事务化/幂等跳闸 | TransactionTemplate 整体事务 + count>0 幂等闸 + `app.seed.enabled` 开关 | ✅ |

## 二、上线验收验证结果

| 验收项 | 结果 |
|---|---|
| 后端测试门禁 | **Tests run 53, Failures 0, Errors 0**（新增 2 用例：跨校绑定拒绝、老师越班录入拒绝） |
| 前端构建 | `npm run build` exit 0 |
| 前端单测 | `npm test` exit 0（会话模型兼容，12/12） |
| 全栈打包 | `mvn package -Dfrontend.bundle.skip=false` → **BUILD SUCCESS**，产物 `backend/target/exam-score-backend-0.0.1-SNAPSHOT.jar`（48.6MB，含前端口语） |

## 三、上线前仍需关注的遗留项（建议作为后续批次，不影响本次审批通过）

1. **JWT 密钥**：上线必须通过环境变量 `APP_SECURITY_TOKEN_SECRET` 显式注入强随机值，**禁止使用默认值公网部署**。
2. **HTTPS 补 Secure**：HttpOnly Cookie 的 `Secure` 标志由反向代理在 HTTPS 侧追加，落地时需配置。
3. **P2 结构/测试（批次4）**：StatisticsService 拆分、前端组件级测试、studentTrend 批量入口、DB 外键。
4. **P3 产品（批次5）**：信息架构、C 类产品清单（档案/批量导入/通知/分享等）——与本次技术整改解耦排期。

## 四、结论

批次1—批次3 六项整改已全部落地并满足各自上线验收标准，后端 53 测试全绿、前端构建/单测通过、单 jar 全栈打包成功。系统已从「可单机上线」推进到「可公网/多实例上线」的安全基线（凭据不可被 XSS 窃取 + 登录/下线/改密集中失效 + CSP/CSRF 纵深防御 + 老师权限全链 + 查询索引 + 播种可控）。**确认满足当前上线要求，予以审批**；详见 §三 遗留项作为后续演进清单。
